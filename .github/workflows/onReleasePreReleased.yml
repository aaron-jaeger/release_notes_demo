name: On Release PreReleased
on:
  release: 
    types: [prereleased,published]
jobs:
  test:
    name: PreReleased test
    runs-on: ubuntu-latest
    steps:
      - name: Echo stuff
        run: |
          echo "prereleased"